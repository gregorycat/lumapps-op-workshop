"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.LumappsStore = void 0;
var widget_playground_context_1 = require("./widget-playground-context");
var utils_1 = require("./utils");
var LumappsStore = (function () {
    function LumappsStore() {
        var _a = window, CONNECTED_USER = _a.CONNECTED_USER, CUSTOMER_ID = _a.CUSTOMER_ID;
        if (!widget_playground_context_1.isPlayground()) {
            if (!CUSTOMER_ID) {
                console.error('[lumapps-sdk-js] CUSTOMER_ID global variable is not available');
            }
            if (!CONNECTED_USER) {
                console.error('[lumapps-sdk-js] CONNECTED_USER global variable is not available');
            }
        }
        this._connectedUser = CONNECTED_USER;
        this._customerId = CUSTOMER_ID;
    }
    Object.defineProperty(LumappsStore.prototype, "connectedUser", {
        get: function () {
            return utils_1.inPromise(this._connectedUser);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(LumappsStore.prototype, "userLang", {
        get: function () {
            var _a;
            return utils_1.inPromise((_a = this._connectedUser) === null || _a === void 0 ? void 0 : _a.lang);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(LumappsStore.prototype, "lumappsToken", {
        get: function () {
            var _a;
            return utils_1.inPromise((_a = this._connectedUser) === null || _a === void 0 ? void 0 : _a.token);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(LumappsStore.prototype, "customerId", {
        get: function () {
            return utils_1.inPromise(this._customerId);
        },
        enumerable: false,
        configurable: true
    });
    return LumappsStore;
}());
exports.LumappsStore = LumappsStore;
