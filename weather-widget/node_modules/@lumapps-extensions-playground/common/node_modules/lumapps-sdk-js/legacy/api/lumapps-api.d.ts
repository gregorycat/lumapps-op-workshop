import 'whatwg-fetch';
export declare class LumappsApi {
    hostName: string;
    token?: string;
    constructor({ hostName, token }: {
        hostName: string;
        token?: string;
    });
    getAuthorizationHeaders(): Promise<string>;
    get({ path }: {
        path: string;
    }): Promise<any>;
    unauthorizedPost({ path }: {
        path: string;
    }): Promise<any>;
    post({ path }: {
        path: string;
    }): Promise<any>;
}
