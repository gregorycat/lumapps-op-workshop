import React from 'react';
import { ErrorModalSize } from '../ErrorModal';
interface PredefinedErrorModalProps {
    size: ErrorModalSize;
    lang?: string;
}
declare const PredefinedErrorModal: ({ size, lang, }: PredefinedErrorModalProps) => React.ReactElement;
export default PredefinedErrorModal;
