import React from 'react';
import { ErrorModalSize, ErrorModalType } from './types';
import './errorModal.scss';
interface ErrorModalBodyProps {
    type: ErrorModalType;
    size: ErrorModalSize;
    message: string | React.ReactNode;
    failOrInfoImageSrc?: string;
}
declare const ErrorModalBody: ({ type, size, message, failOrInfoImageSrc }: ErrorModalBodyProps) => React.ReactElement;
export default ErrorModalBody;
