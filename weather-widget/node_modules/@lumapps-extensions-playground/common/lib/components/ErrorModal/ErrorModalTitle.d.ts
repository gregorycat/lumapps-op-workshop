import React from 'react';
import { ErrorModalSize } from './types';
import './errorModal.scss';
interface ErrorModalTitleProps {
    size: ErrorModalSize;
    title: string | React.ReactNode;
}
declare const ErrorModalTitle: ({ size, title, }: ErrorModalTitleProps) => React.ReactElement;
export default ErrorModalTitle;
