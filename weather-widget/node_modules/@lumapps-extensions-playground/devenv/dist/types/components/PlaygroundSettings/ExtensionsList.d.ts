import React from 'react';
import { Theme } from '@lumx/react';
interface ExtensionListProps {
    environment: string;
    theme: Theme;
    onCloseModal: () => void;
}
declare const ExtensionList: React.FC<ExtensionListProps>;
export { ExtensionList };
