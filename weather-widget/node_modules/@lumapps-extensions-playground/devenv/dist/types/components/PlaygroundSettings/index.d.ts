import React from 'react';
import { Theme } from '@lumx/react';
declare global {
    interface Window {
        APPLICATION: {
            HOST?: string;
            PROTOCOL?: string;
        };
    }
}
interface PlaygroundSettingsDialog {
    config: import('../../configTypes').ExtensionConfig;
    theme: Theme;
}
declare const PlaygroundSettingsDialog: React.FC<PlaygroundSettingsDialog>;
export default PlaygroundSettingsDialog;
