import React from 'react';
declare type ExtensionConfig = import('../../configTypes').ExtensionConfig;
export interface CustomerSelection {
    environment: string;
    customerList: string[];
    selectedCustomers: string[];
    setSelectedCustomers(customers: string[]): void;
}
interface ExtensionSettingsProps {
    environment: string;
    extensionId: string;
    partnerId: string;
    selectedEnv: string;
    onCloseModal: () => void;
    config: ExtensionConfig;
    setExtensionId: (extensionId: string) => void;
}
declare const ExtensionSettings: React.FC<ExtensionSettingsProps>;
export { ExtensionSettings };
