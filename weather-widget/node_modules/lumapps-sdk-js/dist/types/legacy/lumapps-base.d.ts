import { LumappsApi } from './api/lumapps-api';
declare type LumappsBaseOptions = {
    hostName: string;
    token: string;
};
export declare class LumappsBase {
    hostName: string;
    api: LumappsApi;
    constructor(options?: Partial<LumappsBaseOptions>);
}
export {};
